apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: backend-ms
  name: backend-ms
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend-ms
  template:
    metadata:
      labels:
        app: backend-ms
    spec:
      containers:
        - image: quay.io/openshift-pipeline/vote-api:latest
          imagePullPolicy: Always
          name: backend-ms
          ports:
            - containerPort: 9000
              protocol: TCP